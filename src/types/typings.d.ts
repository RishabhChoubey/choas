import { Icon } from "@/components/Icons";

interface SidebarOption {
  id: number;
  name: string;
  href: string;
  Icon: Icon;
}

interface Session {
  id: UserId;
  name: UserId;
  email: UserId;
  image: UserId;
}
